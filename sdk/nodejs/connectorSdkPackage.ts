// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * This resource allows you to upload and manage a Connector SDK code package. The `.zip` file referenced by `filePath` is uploaded to Fivetran, and the resulting package ID can be referenced by a `fivetran.Connector` resource with `service = "connectorSdk"`.
 *
 * The provider computes a SHA-256 hash of the file during `pulumi preview`, so changes to the `.zip` contents are detected without relying on metadata such as modification times.
 *
 * ## Import
 *
 * 1. To import an existing `fivetran.ConnectorSdkPackage` resource into your Terraform state, you need to get the package ID (a two-word identifier, e.g. `distributePanning`) from the Fivetran UI or API.
 *
 * 2. Define an empty resource in your `.tf` configuration:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as fivetran from "@ryan-pip/pulumi-fivetran";
 *
 * const myImportedSdkPackage = new fivetran.ConnectorSdkPackage("my_imported_sdk_package", {});
 * ```
 *
 * 3. Run the `pulumi import` command:
 *
 * ```sh
 * $ pulumi import fivetran:index/connectorSdkPackage:ConnectorSdkPackage my_imported_sdk_package {package_id}
 * ```
 *
 * 4. Use the `terraform state show` command to get the values from the state:
 *
 * ```sh
 * terraform state show 'fivetran_connector_sdk_package.my_imported_sdk_package'
 * ```
 *
 * 5. Copy the computed values and paste them into your `.tf` configuration. Then set `filePath` to the local path of the `.zip` file that was originally uploaded. The `filePath` attribute is not returned by the API, so it cannot be populated by import alone — you must supply it manually.
 *
 * > NOTE: After import, the first `pulumi preview` may show a non-destructive in-place update for `filePath`. As long as the SHA-256 hash of the local file matches the imported `fileSha256Hash`, the subsequent `apply` will only reconcile state and will not re-upload the file.
 */
export class ConnectorSdkPackage extends pulumi.CustomResource {
    /**
     * Get an existing ConnectorSdkPackage resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ConnectorSdkPackageState, opts?: pulumi.CustomResourceOptions): ConnectorSdkPackage {
        return new ConnectorSdkPackage(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'fivetran:index/connectorSdkPackage:ConnectorSdkPackage';

    /**
     * Returns true if the given object is an instance of ConnectorSdkPackage.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ConnectorSdkPackage {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ConnectorSdkPackage.__pulumiType;
    }

    /**
     * Timestamp when the package was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * Path to the .zip file to upload. File is read during plan (for change detection) and during apply (for upload).
     */
    declare public readonly filePath: pulumi.Output<string>;
    /**
     * SHA-256 hash of the uploaded file as computed and stored by the API. Used for upstream drift detection.
     */
    declare public /*out*/ readonly fileSha256Hash: pulumi.Output<string>;
    /**
     * Timestamp when the package was last updated.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;

    /**
     * Create a ConnectorSdkPackage resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ConnectorSdkPackageArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ConnectorSdkPackageArgs | ConnectorSdkPackageState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ConnectorSdkPackageState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["filePath"] = state?.filePath;
            resourceInputs["fileSha256Hash"] = state?.fileSha256Hash;
            resourceInputs["updatedAt"] = state?.updatedAt;
        } else {
            const args = argsOrState as ConnectorSdkPackageArgs | undefined;
            if (args?.filePath === undefined && !opts.urn) {
                throw new Error("Missing required property 'filePath'");
            }
            resourceInputs["filePath"] = args?.filePath;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["fileSha256Hash"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ConnectorSdkPackage.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ConnectorSdkPackage resources.
 */
export interface ConnectorSdkPackageState {
    /**
     * Timestamp when the package was created.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * Path to the .zip file to upload. File is read during plan (for change detection) and during apply (for upload).
     */
    filePath?: pulumi.Input<string | undefined>;
    /**
     * SHA-256 hash of the uploaded file as computed and stored by the API. Used for upstream drift detection.
     */
    fileSha256Hash?: pulumi.Input<string | undefined>;
    /**
     * Timestamp when the package was last updated.
     */
    updatedAt?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ConnectorSdkPackage resource.
 */
export interface ConnectorSdkPackageArgs {
    /**
     * Path to the .zip file to upload. File is read during plan (for change detection) and during apply (for upload).
     */
    filePath: pulumi.Input<string>;
}
