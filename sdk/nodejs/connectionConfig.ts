// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * This resource allows you to manage connection configuration (config and auth fields) separately from the connection structure.
 *
 * The `fivetran.ConnectionConfig` resource configures connection-specific details like host, port, database credentials, and authentication settings. Use this in combination with `fivetran.Connection` for flexible connection management.
 *
 * ## Example Usage
 *
 * ### PostgreSQL Connection with Config and Auth
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as fivetran from "@ryan-pip/pulumi-fivetran";
 *
 * const postgres = new fivetran.Connection("postgres", {
 *     groupId: example.id,
 *     service: "postgres",
 *     destinationSchema: {
 *         prefix: "my_postgres",
 *     },
 *     runSetupTests: false,
 * });
 * const postgresConnectionConfig = new fivetran.ConnectionConfig("postgres", {
 *     connectionId: postgres.id,
 *     config: JSON.stringify({
 *         update_method: "XMIN",
 *         host: databaseHost,
 *         port: 5432,
 *         database: "production_db",
 *     }),
 *     auth: JSON.stringify({
 *         user: dbUser,
 *         password: dbPassword,
 *     }),
 *     runSetupTests: true,
 *     trustCertificates: false,
 *     trustFingerprints: false,
 * });
 * ```
 *
 * ### MySQL Connection with Config Only
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as fivetran from "@ryan-pip/pulumi-fivetran";
 *
 * const mysql = new fivetran.Connection("mysql", {
 *     groupId: example.id,
 *     service: "mysql",
 *     destinationSchema: {
 *         prefix: "my_mysql",
 *     },
 * });
 * const mysqlConnectionConfig = new fivetran.ConnectionConfig("mysql", {
 *     connectionId: mysql.id,
 *     config: JSON.stringify({
 *         update_method: "BINLOG",
 *         host: "mysql.example.com",
 *         port: 3306,
 *         database: "mydb",
 *         user: "fivetran_user",
 *         password: mysqlPassword,
 *     }),
 *     runSetupTests: true,
 * });
 * ```
 *
 * ### S3 Connection with Auth Only
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as fivetran from "@ryan-pip/pulumi-fivetran";
 *
 * const s3 = new fivetran.Connection("s3", {
 *     groupId: example.id,
 *     service: "s3",
 *     destinationSchema: {
 *         name: "s3_data",
 *     },
 * });
 * const s3ConnectionConfig = new fivetran.ConnectionConfig("s3", {
 *     connectionId: s3.id,
 *     auth: JSON.stringify({
 *         role_arn: "arn:aws:iam::123456789:role/fivetran-access",
 *     }),
 *     runSetupTests: true,
 * });
 * ```
 *
 * ### Connection with Sensitive Credentials
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as fivetran from "@ryan-pip/pulumi-fivetran";
 *
 * const postgresSecure = new fivetran.ConnectionConfig("postgres_secure", {
 *     connectionId: postgresFivetranConnection.id,
 *     config: JSON.stringify({
 *         update_method: "XMIN",
 *         host: postgres.address,
 *         port: postgres.port,
 *         database: postgres.dbName,
 *     }),
 *     auth: JSON.stringify({
 *         user: dbCreds.secretString.username,
 *         password: dbCreds.secretString.password,
 *     }),
 *     runSetupTests: true,
 *     trustCertificates: true,
 * });
 * ```
 *
 * ## Notes
 *
 * - **Semantic JSON Equality:** The `config` and `auth` fields use semantic JSON comparison, meaning changes in whitespace, key ordering, or formatting won't trigger unnecessary updates.
 * - **Separation of Concerns:** This resource allows you to manage connection configuration separately from the connection structure, enabling better security practices and workflow flexibility.
 * - **Setup Tests:** When `runSetupTests` is `true`, Fivetran will validate the configuration. Any test failures will appear as warnings in the Terraform output.
 * - **Sensitive Data:** Consider using Terraform's `sensitive` attribute or external secret management for credentials in the `auth` field.
 * - **Configuration Updates:** Both `config` and `auth` are optional. You can update one without affecting the other.
 *
 * ## See Also
 *
 * - `fivetran.Connection` - Create the connection structure
 * - [Fivetran API Documentation](https://fivetran.com/docs/rest-api/connectors) - Connection configuration reference
 *
 * ## Import
 *
 * Connection configurations can be imported using the connection ID:
 *
 * ```sh
 * $ pulumi import fivetran:index/connectionConfig:ConnectionConfig example connection_id_here
 * ```
 *
 * **Note:** When importing, the `runSetupTests`, `trustCertificates`, and `trustFingerprints` attributes will not be imported as they are plan-only attributes.
 */
export class ConnectionConfig extends pulumi.CustomResource {
    /**
     * Get an existing ConnectionConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ConnectionConfigState, opts?: pulumi.CustomResourceOptions): ConnectionConfig {
        return new ConnectionConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'fivetran:index/connectionConfig:ConnectionConfig';

    /**
     * Returns true if the given object is an instance of ConnectionConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ConnectionConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ConnectionConfig.__pulumiType;
    }

    /**
     * Connection auth config in Json format, following [Fivetran API endpoint contract](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection) for `auth` field. This field uses semantic JSON equality, so whitespace and key order differences won't trigger updates.
     */
    declare public readonly auth: pulumi.Output<string | undefined>;
    /**
     * Connection config in Json format, following [Fivetran API endpoint contract](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection) for `config` field. This field uses semantic JSON equality, so whitespace and key order differences won't trigger updates.
     */
    declare public readonly config: pulumi.Output<string | undefined>;
    /**
     * The unique identifier for the connection within the Fivetran system.
     */
    declare public readonly connectionId: pulumi.Output<string>;
    /**
     * Whether to run setup tests when applying configuration. Default: `false`. When `true`, Fivetran validates the configuration by testing the connection. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    declare public readonly runSetupTests: pulumi.Output<boolean>;
    /**
     * Whether to automatically trust SSL certificates. Default: `false`. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    declare public readonly trustCertificates: pulumi.Output<boolean>;
    /**
     * Whether to automatically trust SSH fingerprints. Default: `false`. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    declare public readonly trustFingerprints: pulumi.Output<boolean>;

    /**
     * Create a ConnectionConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ConnectionConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ConnectionConfigArgs | ConnectionConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ConnectionConfigState | undefined;
            resourceInputs["auth"] = state?.auth;
            resourceInputs["config"] = state?.config;
            resourceInputs["connectionId"] = state?.connectionId;
            resourceInputs["runSetupTests"] = state?.runSetupTests;
            resourceInputs["trustCertificates"] = state?.trustCertificates;
            resourceInputs["trustFingerprints"] = state?.trustFingerprints;
        } else {
            const args = argsOrState as ConnectionConfigArgs | undefined;
            if (args?.connectionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'connectionId'");
            }
            resourceInputs["auth"] = args?.auth;
            resourceInputs["config"] = args?.config;
            resourceInputs["connectionId"] = args?.connectionId;
            resourceInputs["runSetupTests"] = args?.runSetupTests;
            resourceInputs["trustCertificates"] = args?.trustCertificates;
            resourceInputs["trustFingerprints"] = args?.trustFingerprints;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ConnectionConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ConnectionConfig resources.
 */
export interface ConnectionConfigState {
    /**
     * Connection auth config in Json format, following [Fivetran API endpoint contract](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection) for `auth` field. This field uses semantic JSON equality, so whitespace and key order differences won't trigger updates.
     */
    auth?: pulumi.Input<string | undefined>;
    /**
     * Connection config in Json format, following [Fivetran API endpoint contract](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection) for `config` field. This field uses semantic JSON equality, so whitespace and key order differences won't trigger updates.
     */
    config?: pulumi.Input<string | undefined>;
    /**
     * The unique identifier for the connection within the Fivetran system.
     */
    connectionId?: pulumi.Input<string | undefined>;
    /**
     * Whether to run setup tests when applying configuration. Default: `false`. When `true`, Fivetran validates the configuration by testing the connection. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    runSetupTests?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to automatically trust SSL certificates. Default: `false`. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    trustCertificates?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to automatically trust SSH fingerprints. Default: `false`. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    trustFingerprints?: pulumi.Input<boolean | undefined>;
}

/**
 * The set of arguments for constructing a ConnectionConfig resource.
 */
export interface ConnectionConfigArgs {
    /**
     * Connection auth config in Json format, following [Fivetran API endpoint contract](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection) for `auth` field. This field uses semantic JSON equality, so whitespace and key order differences won't trigger updates.
     */
    auth?: pulumi.Input<string | undefined>;
    /**
     * Connection config in Json format, following [Fivetran API endpoint contract](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection) for `config` field. This field uses semantic JSON equality, so whitespace and key order differences won't trigger updates.
     */
    config?: pulumi.Input<string | undefined>;
    /**
     * The unique identifier for the connection within the Fivetran system.
     */
    connectionId: pulumi.Input<string>;
    /**
     * Whether to run setup tests when applying configuration. Default: `false`. When `true`, Fivetran validates the configuration by testing the connection. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    runSetupTests?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to automatically trust SSL certificates. Default: `false`. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    trustCertificates?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to automatically trust SSH fingerprints. Default: `false`. **Note:** This is a plan-only attribute and will not be stored in state.
     */
    trustFingerprints?: pulumi.Input<boolean | undefined>;
}
